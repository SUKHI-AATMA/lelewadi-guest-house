const colors = require('tailwindcss/colors')

module.exports = {
    theme: {
        colors: {
            secondary: colors.indigo,
            primary: colors.yellow,
            neutral: colors.gray,
        }
    }
}